# AI Experiments

This repository is used as a code-only transfer and experiment hub.

## Layout

- `experiments/tent/`: Tent paper code and configs

## Notes

- Keep datasets, checkpoints, and run outputs out of git.
- Add each new experiment under its own subdirectory in `experiments/`.
